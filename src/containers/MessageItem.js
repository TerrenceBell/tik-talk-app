import React, { Component } from './react'; 


class MessageItem extends Component { 


    render() { 
        return( 
            null
        )
    }
}

export default MessageItem


//{this.props.comments.map((comment, i) => (          <CommentCard            key={comment.id}            removeComment={this.props.removeComment}            comment={comment}          />        ))}